# AI Study Planner - Frontend (Phase 6) Skeleton

This is a minimal scaffold for the Phase 6 frontend implementation. It is intentionally lightweight and relies on the existing monorepo tooling. Once dependencies are installed, this app should be runnable via `npm run dev` from the workspace root.
